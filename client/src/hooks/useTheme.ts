import { useContext } from "react";
import { ThemeProvider } from "@/components/theme-provider";

// Re-export the useTheme hook from the theme provider
export { useTheme } from "@/components/theme-provider";
